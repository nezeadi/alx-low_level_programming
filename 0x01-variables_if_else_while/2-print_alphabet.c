#include <stdio.h>
/**
*main - a program to print eh alphabet in lowercase
*Return: Always 0
*/

int main(void)
{
	char letter;

	while (letter < 'z')
	{
		putchar(letter\n);
		putchar('\n');
		letter++
	}
	return (0);
}
