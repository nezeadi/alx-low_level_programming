#include <stdio.h>

/**
 * main - a program that prints 0-9
 * Return: Always 0
 */

int main(void)
{
	int i = 0;

	while (i <= 9)
	{
		printf("%d", i);
		i++;
	}
        printf("\n");
	return (0);
}
